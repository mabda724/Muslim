<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/navy_900">

    <!-- Glow Effects -->
    <View
        android:layout_width="400dp"
        android:layout_height="400dp"
        android:layout_gravity="top|center_horizontal"
        android:layout_marginTop="-100dp"
        android:background="@drawable/glow_emerald"
        android:alpha="0.5" />

    <View
        android:layout_width="300dp"
        android:layout_height="300dp"
        android:layout_gravity="bottom|end"
        android:layout_marginEnd="-80dp"
        android:layout_marginBottom="-80dp"
        android:background="@drawable/glow_gold"
        android:alpha="0.3" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="32dp">

        <!-- Mosque Icon -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="🕌"
            android:textSize="72sp"
            android:layout_marginBottom="16dp" />

        <!-- "Now" label -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="الآن موعد صلاة"
            android:textColor="@color/text_secondary"
            android:textSize="20sp"
            android:layout_marginBottom="8dp" />

        <!-- Prayer Name -->
        <TextView
            android:id="@+id/tvPrayerName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="الفجر"
            android:textColor="@color/gold_500"
            android:textSize="52sp"
            android:textStyle="bold"
            android:fontFamily="sans-serif-medium"
            android:layout_marginBottom="24dp" />

        <!-- Adhan Time -->
        <TextView
            android:id="@+id/tvPrayerTime"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="04:30"
            android:textColor="@color/text_primary"
            android:textSize="28sp"
            android:layout_marginBottom="32dp" />

        <!-- Progress Bar for audio -->
        <ProgressBar
            android:id="@+id/progressAdhan"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="4dp"
            android:max="100"
            android:progress="0"
            android:progressTint="@color/gold_500"
            android:progressBackgroundTint="#33FFFFFF"
            android:layout_marginBottom="32dp" />

        <!-- Controls Row -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center"
            android:layout_marginBottom="40dp">

            <!-- Play/Pause Button -->
            <FrameLayout
                android:id="@+id/btnPlayPause"
                android:layout_width="72dp"
                android:layout_height="72dp"
                android:background="@drawable/circle_button_gold"
                android:layout_marginEnd="24dp"
                android:foreground="?android:selectableItemBackground">

                <TextView
                    android:id="@+id/tvPlayPause"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center"
                    android:text="⏸"
                    android:textSize="32sp"
                    android:textColor="@color/navy_900" />
            </FrameLayout>

            <!-- Stop Button -->
            <FrameLayout
                android:id="@+id/btnStop"
                android:layout_width="72dp"
                android:layout_height="72dp"
                android:background="@drawable/circle_button_glass"
                android:foreground="?android:selectableItemBackground">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center"
                    android:text="⏹"
                    android:textSize="32sp"
                    android:textColor="@color/danger_red" />
            </FrameLayout>
        </LinearLayout>

        <!-- Dua text -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="اللهم صل وسلم على نبينا محمد"
            android:textColor="@color/text_secondary"
            android:textSize="16sp"
            android:gravity="center"
            android:layout_marginBottom="8dp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="ادعو للمطور محمد ابراهيم عبدالله 🤲"
            android:textColor="@color/text_muted"
            android:textSize="13sp"
            android:gravity="center" />

    </LinearLayout>
</FrameLayout>
